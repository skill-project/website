<?php

    namespace Config;

    class Config {

        const BASE_URL		= _BASE_URL;

        const NEO_HOST      = _NEO_HOST;
        const NEO_PORT      = _NEO_PORT;
        
        const DEBUG         = _DEBUG;
        
    }